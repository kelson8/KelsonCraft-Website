# Taken this from my Nginx docker image

# https://deployn.de/en/blog/astrojs-docker/

# I got this working with a non-root user.
# user  nginx;
worker_processes  auto;

error_log  /var/log/nginx/error.log notice;
# pid        /var/run/nginx.pid;

# New for non-root setup
pid /tmp/nginx.pid;


events {
    worker_connections  1024;
}


http {

    root /var/www/html;

    # New for non-root setup
    # https://github.com/nginx/docker-nginx-unprivileged#troubleshooting-tips
    client_body_temp_path /tmp/client_temp;
    proxy_temp_path       /tmp/proxy_temp_path;
    fastcgi_temp_path     /tmp/fastcgi_temp;
    uwsgi_temp_path       /tmp/uwsgi_temp;
    scgi_temp_path        /tmp/scgi_temp;

    charset                utf-8;
    sendfile               on;
    tcp_nopush             on;
    tcp_nodelay            on;
    server_tokens          off;
    log_not_found          off;
    types_hash_max_size    2048;
    types_hash_bucket_size 64;
    client_max_body_size   16M;

    # MIME
    include                mime.types;
    default_type           application/octet-stream;

    # Logging
    access_log  /var/log/nginx/access.log;
    error_log /var/log/nginx/error.log;
    # Use this to
#    error_log              /dev/null;


    #------------
    # Disabled below for this docker image for now, not sure if this is cauing problems
    #------------
    #
    # SSL

#     ssl_session_timeout    1d;
#     ssl_session_cache      shared:SSL:10m;
#     ssl_session_tickets    off;

    # Diffie-Hellman parameter for DHE ciphersuites
    #ssl_dhparam            /etc/nginx/dhparam.pem;

    # Mozilla Intermediate configuration
#     ssl_protocols          TLSv1.2 TLSv1.3;
#     ssl_ciphers            ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384:ECDHE-ECDSA-CHACHA20-POLY1305:ECDHE-RSA-CHACHA20-POLY1305:DHE-RSA-AES128-GCM-SHA256:DHE-RSA-AES256-GCM-SHA384;

    # OCSP Stapling
#    ssl_stapling           on;
#    ssl_stapling_verify    on;
#     resolver               1.1.1.1 1.0.0.1 valid=60s;
#     resolver_timeout       2s;

    #------------
    #
    #------------

    # Logger format
    # TODO Fix this to work with Cloudflare later.
    log_format  main  '$remote_addr - $remote_user [$time_local] "$request" '
                      '$status $body_bytes_sent "$http_referer" '
                      '"$http_user_agent" "$http_x_forwarded_for"';

    keepalive_timeout  65;

    #gzip  on;

    #Load configs
    include /etc/nginx/conf.d/*.conf;
}


###